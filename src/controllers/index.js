/**
 * Exportaciones del módulo de controladores
 */

const matchmakingController = require('./matchmakingController');
const healthController = require('./healthController');

module.exports = {
  matchmakingController,
  healthController,
};